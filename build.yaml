---
name: "Attachment Optimizer"
guid: "41341b7d-9374-4c82-824a-21d360036771"
version: "1.0.0.0"
targetAbi: "12.0.0.0"
framework: "net10.0"
overview: "Optimizes Jellyfin attachment extraction, deduplication, and cache cleanup."
description: >
  Batches missing attachment extraction into one FFmpeg invocation, stores
  identical attachment bytes once, preserves detected font formats, and safely
  cleans plugin-managed cache data.
category: "General"
owner: "KimPig"
artifacts:
- "Jellyfin.Plugin.AttachmentOptimizer.dll"
changelog: |-
  - Batch missing attachments in one FFmpeg process.
  - Deduplicate content with SHA-256 and hard links.
  - Preserve detected font formats and manage cache cleanup.
